//camera
// let targetScale = 0.7; // Target scale factor
// let currentScale = 1; // Initial scale factor
// let currentX = 0;
// let currentY = 0;
// let easing = 0.3; // Easing value (adjust for desired smoothness)
// };
// window.draw = function draw() {
// console.log(degrees(player.theta));
// let angle = degrees(player.theta);

// if ((angle > -130 && angle < -40) || (angle < 140 && angle > 40)) {
//   // Scale the drawing by 0.7 in both x and y directions
//   currentScale = lerp(currentScale, targetScale, easing);
//   currentX = lerp(currentX, c.canvasWidth / 7, easing);
//   currentY = lerp(currentY, c.canvasHeight / 7, easing);
// } else {
//   // Scale back to normal
//   currentScale = lerp(currentScale, 1, easing);
//   currentX = lerp(currentX, 0, easing);
//   currentY = lerp(currentY, 0, easing);
// }

// // Apply translation and scale
// translate(currentX, currentY);
// scale(currentScale);
